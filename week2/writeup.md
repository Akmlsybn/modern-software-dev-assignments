# Week 2 Write-up

Tip: To preview this markdown file

- On Mac, press `Command (⌘) + Shift + V`
- On Windows/Linux, press `Ctrl + Shift + V`

## INSTRUCTIONS

Fill out all of the `TODO`s in this file.

## SUBMISSION DETAILS

Name: Akmallullail Sya'ban \
SUNet ID: 2310817310010 \
Citations: GitHub Copilot (Inline & Chat Workspace), Ollama (llama3.1:8b model), and Gemini (for AI pair-programming troubleshooting)

This assignment took me about 4 hours to do.

## YOUR RESPONSES

For each exercise, please include what prompts you used to generate the answer, in addition to the location of the generated response. Make sure to clearly add comments in your code documenting which parts are generated.

### Exercise 1: Scaffold a New Feature

Prompt:

```text
Buatkan fungsi extract_action_items_llm(text: str) -> List[str] yang memanggil model Ollama secara lokal (model llama3.1:8b dengan temperature 0.0) untuk mengekstrak action items dari teks. Pastikan output yang dihasilkan wajib berbentuk JSON (array of strings). Tambahkan komentar inline '# --- GENERATED BY AI ---' di awal dan akhir fungsi ini.
```

Generated Code Snippets:

- week2/app/services/extract.py: Menambahkan fungsi `extract_action_items_llm()` di bagian bawah file untuk memanggil Ollama secara lokal.

### Exercise 2: Add Unit Tests

Prompt:

```
Buatkan pytest unit tests untuk fungsi extract_action_items_llm() dari modul app.services.extract. Buat 3 fungsi test: satu untuk input berisi bullet lists, satu untuk input dengan keyword TODO atau ACTION, dan satu untuk input string kosong (""). Tambahkan penanda komentar '# --- GENERATED BY AI ---' di awal dan akhir blok kode ini.
```

Generated Code Snippets:

- week2/tests/test_extract.py: Menambahkan 3 fungsi pengujian (`test_extract_action_items_llm_with_bullets`, `test_extract_action_items_llm_with_keywords`, `test_extract_action_items_llm_empty_string`) di bagian bawah file.

### Exercise 3: Refactor Existing Code for Clarity

Prompt:

```

Bantu refactor kode backend agar lebih rapi. Fokus pada 3 file yang sudah saya lampirkan (main.py, db.py, action_items.py). Tolong perbaiki struktur API schemas, rapikan bagian database (SQLite), dan tambahkan error handling yang sesuai untuk setiap endpoint. Sertakan komentar '# --- GENERATED BY AI ---' di bagian-bagian yang kamu ubah.

```

Generated/Modified Code Snippets:

- week2/app/schemas.py: (File Baru) Membuat struktur Pydantic models (NoteRead, ActionItemRead, ExtractRequest/Response).
- week2/app/db.py: Menambahkan Context Manager (with sqlite3.connect) dan validasi database.
- week2/app/routers/action_items.py: Mengimplementasikan Pydantic schemas dan menambahkan HTTP error handling (400, 404, 500).
- week2/app/main.py: Menambahkan metadata FastAPI dan exception handlers.

### Exercise 4: Use Agentic Mode to Automate a Small Task

Prompt:

```
Tambahkan satu endpoint POST baru di action_items.py untuk menjalankan ekstraksi menggunakan LLM (gunakan fungsi extract_action_items_llm dari services.extract). Pastikan juga ada endpoint GET untuk mengambil seluruh data catatan (List Notes). Setelah itu, update file index.html di bagian frontend untuk menambahkan tombol 'Extract LLM' dan 'List Notes' beserta logic JavaScript (fetch) pemanggilannya. Tolong tambahkan komentar '# --- GENERATED BY AI ---' (atau untuk HTML) di bagian yang diubah.
```

Generated Code Snippets:

- week2/app/routers/action_items.py: Menambahkan endpoint `POST /action-items/extract-llm`.
- week2/app/routers/notes.py: Menambahkan endpoint `GET /notes/` untuk list catatan.
- week2/frontend/index.html: Menambahkan tombol UI "Extract LLM" dan "List Notes" beserta fungsi helper JavaScript `renderItems()` dan `extractItems()`.

### Exercise 5: Generate a README from the Codebase

Prompt:

```
Tolong analisis 3 file yang saya lampirkan ini (pyproject.toml, main.py, action_items.py) dan buatkan dokumentasi proyek dalam bentuk README.md. Pastikan isinya mencakup: 1. Ringkasan proyek (Project Overview). 2. Cara setup dan menjalankan aplikasi menggunakan conda, poetry, uvicorn, dan ollama. 3. Daftar API endpoint beserta fungsinya. 4. Petunjuk cara menjalankan unit test dengan pytest. Outputkan dalam format Markdown yang rapi.
```

Generated Code Snippets:

- README.md: (File Baru) Membuat dokumentasi lengkap proyek berdasarkan hasil analisis otomatis dari Copilot.

## SUBMISSION INSTRUCTIONS

1. Hit a `Command (⌘) + F` (or `Ctrl + F`) to find any remaining `TODO`s in this file. If no results are found, congratulations – you've completed all required fields.
2. Make sure you have all changes pushed to your remote repository for grading.
3. Submit via Gradescope.
